# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.ruff_cache/
.mypy_cache/
*.egg-info/
.venv/
venv/

# PHP (installed via `composer install` in scanner/php/ -- see README.md)
scanner/php/vendor/

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# Scanner output (generated by running the scanner locally -- these are
# working artifacts, never committed)
reports/
*.security-baseline.json

# Local-only test fixtures: a deliberately vulnerable WooCommerce plugin
# used to sanity-check lint/unit/security CI locally before pushing (run
# the scanner against local-test-fixtures/wc-vulnerable-test-plugin and
# confirm it finds the expected findings). Never committed or shipped --
# even in its neutralized form (dangerous function calls removed, see its
# own README.md), a repo containing deliberately vulnerable PHP is not
# something to publish or hand to anyone who clones this project.
local-test-fixtures/
